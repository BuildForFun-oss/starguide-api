# CPU-only torch wheel — sentence-transformers pulls in torch transitively,
# and without this it defaults to a multi-GB CUDA build that's unnecessary
# on Railway and slows/bloats every deploy.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.6
uvicorn[standard]==0.34.0
pinecone==5.4.2
sentence-transformers==3.3.1
torch==2.5.1
google-genai==2.8.0
python-dotenv==1.0.1
numpy==1.26.4
pydantic==2.10.4
